import { Wrapper } from "./styled";
import { ProgressCircle } from "./ProgressCircle";

export const Timer = () => {
    return(
        <Wrapper>
            <ProgressCircle/>
        </Wrapper>
    );
};